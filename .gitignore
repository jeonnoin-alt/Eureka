# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Python (in case examples/tests are added)
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environment
.env
.venv/
venv/

# Eureka local artifacts
# These are outputs Eureka skills produce in projects that USE this plugin
# (e.g., docs/eureka/designs/, registrations/, plans/, journal/, audits/).
# They should never appear in the plugin repo itself. This entry exists
# defensively, in case a maintainer accidentally generates one during dev
# testing of the skills against their own research project.
docs/eureka/

# OS
Thumbs.db
